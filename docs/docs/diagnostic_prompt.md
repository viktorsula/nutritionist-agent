ДИАГНОСТИКА РАБОТАЮЩЕГО АГЕНТА

Ты — старший инженер по качеству кода.
Проект уже написан и запущен в тестовом режиме.
Твоя задача — найти все проблемы в существующем коде.
Общайся только на русском языке.

Порядок работы

Сначала молча изучи ВСЕ файлы проекта.
Не начинай отчёт пока не прочитал каждый файл.
Используй find и cat чтобы не пропустить ни одного файла:

bashfind . -name "*.py" | sort
find . -name "*.sql" | sort
find . -name "*.md" | sort
find . -name "*.json" | sort
cat CLAUDE.md
cat docs/progress.md

После изучения всех файлов проведи диагностику по разделам ниже.


РАЗДЕЛ 1: АНАЛИЗ ПОТОКОВ ДАННЫХ

1.1 Карта записи и чтения

Для каждой таблицы БД найди в коде:


Где данные ПИШУТСЯ (INSERT/UPDATE)
Где данные ЧИТАЮТСЯ (SELECT)
Если пишется но не читается → 🔵 МЁРТВЫЕ ДАННЫЕ
Если читается но не пишется → 🔴 ПУСТОЙ ЗАПРОС


Проверь каждую таблицу:
users, clients, client_profiles, wellness_plans, conversations, client_events, nutrition_plans, tasks, notification_schedule, audit_logs, system_settings, client_documents_metadata, knowledge_base_metadata

1.2 Анализ JSONB полей

Найди все места где используется payload_json, metadata_json,
plan_json, supplements_json и другие JSONB поля.
Для каждого:


Какие ключи пишутся при INSERT?
Какие ключи читаются при SELECT?
Есть ли ключи которые пишутся но никогда не парсятся?


1.3 Анализ событий client_events

Найди все места где вызывается log_client_event() или аналог.
Найди все места где читаются события из client_events.
Какие event_type пишутся? Какие читаются?
Есть ли event_type которые объявлены в схеме но нигде не создаются?


РАЗДЕЛ 2: АНАЛИЗ БИЗНЕС-ЛОГИКИ

2.1 Business Rules — полнота проверок

Найди все вызовы функций из business_rules/.
Для каждой критической ситуации проверь:

Аллергия клиента → проверяется ли ВЕЗДЕ где предлагается еда?
  - при анализе фото тарелки
  - при генерации меню
  - при анализе фото холодильника
  - при текстовом запросе рецепта

Неактивная оплата → блокируется ли ДО вызова LLM?

Критические показатели → останавливается ли LLM?

Если проверка есть в одном месте но отсутствует в другом — это дыра.

2.2 Порядок выполнения контекстного стека

В каждом агенте проверь порядок:


Business Rules — должен быть ПЕРВЫМ
Загрузка профиля клиента
История разговоров
Knowledge base
LLM вызов — должен быть ПОСЛЕДНИМ


Если где-то LLM вызывается раньше Business Rules — критическая ошибка.
Если где-то профиль клиента не загружается — опасная дыра.

2.3 Тупики логики

Найди ситуации где агент может зациклиться или зависнуть:


Бесконечные циклы уточнений у клиента
Условия которые никогда не выполнятся
Условия которые всегда выполняются (мертвый код)
Ветки if/else где один из вариантов недостижим
Рекурсия без условия выхода


2.4 Необработанные сценарии

Проверь что происходит если:


Клиент не найден в БД по telegram_id
Профиль клиента есть но client_profiles пустой
У клиента нет активного nutrition_plan
История разговоров пустая (первое сообщение)
Gemini вернул ошибку при анализе фото
Groq недоступен
pgvector вернул 0 результатов
Supabase вернул ошибку соединения


Есть ли try/except для каждого случая?
Что возвращается клиенту при каждой ошибке?


РАЗДЕЛ 3: АНАЛИЗ СОВМЕСТИМОСТИ

3.1 LLM провайдеры

Сравни форматы вызовов Groq / Claude / Gemini в utils/llm.py:


Одинаков ли формат messages?
Одинаково ли обрабатываются ответы?
Одинаково ли обрабатываются ошибки?
Есть ли fallback при недоступности провайдера?


3.2 Telegram → Backend

Проверь обработку всех типов входящих сообщений:


text — обрабатывается?
photo — обрабатывается?
voice — обрабатывается?
document — обрабатывается?
Что происходит с неизвестным типом?


Проверь определение роли:


Как определяется нутрициолог vs клиент?
Что если telegram_id не найден в БД?
Что если один telegram_id у двух записей?


3.3 Supabase и RLS

Проверь с каким ключом работает агент:


service_key (обходит RLS) или anon_key?
Корректно ли это для каждого типа операций?


Найди запросы которые могут упасть из-за RLS:


Агент читает данные клиента — разрешено?
Агент пишет события — разрешено?
Агент читает knowledge_base — разрешено?


3.4 pgvector

Проверь:


Размерность векторов при записи эмбеддингов
Размерность векторов при поиске
Совпадают ли? (должно быть 1536 для ada-002)
Есть ли индекс на векторном поле?
Какой порог similarity используется? Адекватен ли он?



РАЗДЕЛ 4: АНАЛИЗ ПРОИЗВОДИТЕЛЬНОСТИ

4.1 Медленные запросы

Найди запросы к БД внутри циклов (N+1 проблема):

python# Пример N+1:
for client in clients:
    profile = db.get_profile(client.id)  # N запросов вместо 1 JOIN

Найди запросы без индексов на часто используемых полях:


WHERE client_id = ? — есть индекс?
ORDER BY created_at DESC — есть индекс?
WHERE is_active = true — есть индекс?


4.2 Размер контекста LLM

Найди где формируется контекст для LLM.
Подсчитай примерный размер в токенах:


Системный промпт: ~? токенов
Профиль клиента: ~? токенов
История last_n сообщений: ~? токенов
Knowledge base chunks: ~? токенов
Итого: превышает ли лимит модели?


4.3 Синхронные блокировки

Найди места где агент ждёт долгих операций синхронно:


Вызов Whisper для транскрипции голоса
Вызов Gemini для анализа фото
Загрузка и эмбеддинг документа
Есть ли async/await? Или всё синхронно и блокирует поток?



РАЗДЕЛ 5: АНАЛИЗ БЕЗОПАСНОСТИ

5.1 Секреты и ключи

bashgrep -r "sk-" . --include="*.py"
grep -r "key=" . --include="*.py"
grep -r "password" . --include="*.py"
grep -r "token" . --include="*.py" | grep -v ".env"

Есть ли захардкоженные секреты?

5.2 Изоляция клиентов

Найди все запросы к БД из клиентской ветки агента.
В каждом запросе есть ли фильтр WHERE client_id = ?
Может ли клиент получить данные другого клиента через агента?

5.3 Валидация входных данных

Что происходит если клиент отправляет:


Очень длинное сообщение (10000 символов)
SQL инъекцию в тексте
Вредоносный файл вместо фото еды
Сообщение на неожиданном языке



РАЗДЕЛ 6: АНАЛИЗ МОНИТОРИНГА

6.1 Покрытие Langfuse

Найди все вызовы LLM в коде.
Каждый ли вызов обёрнут в Langfuse трейсинг?
Есть ли вызовы LLM без мониторинга?

6.2 Логирование ошибок

Найди все блоки except.
Логируются ли ошибки или просто проглатываются?

python# Плохо — ошибка проглочена:
except Exception:
    pass

# Хорошо:
except Exception as e:
    logger.error(f"Описание: {e}")
    # обработка

6.3 audit_logs

Найди все места где данные изменяются (UPDATE/DELETE).
Каждое ли изменение записывается в audit_logs?
Что не логируется но должно?


ФОРМАТ ОТЧЁТА

После анализа ВСЕХ файлов выдай структурированный отчёт:

🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ

Блокируют корректную работу. Исправить немедленно.
Для каждой: файл + строка, описание, последствия, решение.

🟡 ЛОГИЧЕСКИЕ ДЫРЫ

Не блокируют сейчас, но дадут сбой в реальных сценариях.
Для каждой: сценарий который сломается, где в коде, решение.

🔵 МЁРТВЫЕ БЛОКИ

Данные которые пишутся но не читаются (или наоборот).
Для каждого: таблица/поле/функция, что делать — удалить или дописать.

🟠 ПРОБЛЕМЫ ПРОИЗВОДИТЕЛЬНОСТИ

То что будет медленно при 50+ клиентах.
Для каждой: где, почему медленно, как ускорить.

⚪ ТЕХНИЧЕСКИЙ ДОЛГ

Код который работает но написан неправильно.
Можно исправить позже.

✅ ЧТО РАБОТАЕТ ХОРОШО

Что точно не трогать.


В КОНЦЕ ОТЧЁТА

Дай приоритизированный план исправлений:

СЕГОДНЯ (критично):
1. ...
2. ...

НА ЭТОЙ НЕДЕЛЕ (важно):
1. ...
2. ...

ПОТОМ (улучшения):
1. ...

И одну строку: общая оценка готовности системы к реальным клиентам (%).