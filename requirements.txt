# =====================================================================
# Текущая архитектура: бэкенд = FastAPI (api/main.py, запуск uvicorn — см. Dockerfile).
# UI = React-SPA (frontend/, отдельный деплой). Канал клиента = Telegram-бот.
# =====================================================================

# API для веб-фронта (React) + точка входа Docker (uvicorn api.main:app)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # загрузка файлов (фото/голос/PDF)

# Streamlit (app.py, web/*.py) — LEGACY, в Docker НЕ запускается. УДАЛЁН из зависимостей:
# streamlit 1.32 требует protobuf<5, что конфликтует с langgraph/google-generativeai (protobuf>=5).
# Если понадобится запускать старый Streamlit-UI — ставить отдельно (pip install streamlit) вне этого деплоя.

# LLM провайдеры (Этап 4: utils/llm.py)
groq>=0.4.0
anthropic>=0.18.0
google-generativeai>=0.3.0
# OpenAI: эмбеддинги text-embedding-ada-002 (1536) + Whisper (голос) — Этап 6
openai>=1.12.0

# Оркестрация агентов (Этап 6: модернизация стека LangGraph)
# Код использует только langgraph.graph.StateGraph/END.
# langchain/langchain-groq/langsmith в коде НЕ используются (llm.py на «сырых» SDK groq/anthropic/genai).
langgraph>=1.0,<2
langchain-core>=0.3,<0.4

# База данных
supabase==2.10.0

# Векторная база знаний
# Используем pgvector в Supabase + эмбеддинги OpenAI (ada-002, 1536).
# chromadb/sentence-transformers НЕ нужны (их размерность не совпадает с vector(1536)).

# Telegram Bot
# 21.x использует httpx~=0.27 — совместимо с supabase (httpx>=0.26,<0.28).
# Пин 20.7 (httpx~=0.25.2) давал ResolutionImpossible с supabase 2.10.
python-telegram-bot>=21,<22

# Документы
pypdf==4.1.0

# Мониторинг и трейсинг
langfuse>=2.0.0
# langsmith ставится транзитивно langchain-core (без жёсткого пина, чтобы не конфликтовать)

# Веб-поиск: серверный инструмент Claude web_search (utils/web_access.py).
# Поиск выполняет Anthropic — отдельный поисковый SDK/ключ не нужен.
# Требуется включить web search в Claude Console (Settings → Privacy).

# Утилиты
python-dotenv==1.0.1
pytz==2024.1
